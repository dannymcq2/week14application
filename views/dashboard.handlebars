<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Your Dashboard</h1>
    <a href="/NewPost">Create New Post</a>
    <ul>
        {{#each posts}}
            <li>
                <h2>{{this.title}}</h2>
                <p>{{this.content}}</p>
                <a href="/dashboard/edit/{{this.id}}">Edit</a>
                <form action="/api/posts/{{this.id}}" method="POST" style="display:inline;">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit">Delete</button>
                </form>
            </li>
        {{/each}}
    </ul>
</body>
</html>
=======
<h1>Your Dashboard</h1>
<a href="/dashboard/new">Create New Post</a>
<ul>
    {{#each posts}}
        <li>
            <h2>{{this.title}}</h2>
            <p>{{this.content}}</p>
            <a href="/dashboard/edit/{{this.id}}">Edit</a>
            <form action="/api/posts/{{this.id}}" method="POST">
                <input type="hidden" name="_method" value="DELETE">
                <button type="submit">Delete</button>
            </form>
        </li>
    {{/each}}
</ul>

